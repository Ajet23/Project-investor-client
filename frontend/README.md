# Idea Platform - Module 1 (Auth & Onboarding)

## Backend
- Copy `.env.example` to `.env` and set secrets.
- Run: `npm i && npm run dev`

## Frontend
- Copy `.env.example` to `.env` and set `VITE_API_BASE` (defaults to http://localhost:5000/api)
- Run: `npm i && npm run dev`
